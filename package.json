{
  "name": "housemaster-ops",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=20"
  },
  "scripts": {
    "dev:server": "node --watch server/index.js",
    "dev:web": "npm --prefix web run dev",
    "build": "npm --prefix web install --no-audit --no-fund && npm --prefix web run build",
    "start": "node server/index.js",
    "test": "node field/qa-guard.test.js && node server/radonIntake.test.js && node server/attachments.test.js && node server/isnShapes.test.js && node server/zone.test.js && node server/qr.test.js && node server/import.test.js && node server/kitClaim.test.js && node server/workbook.test.js",
    "seed:demo": "node scripts/seed.js",
    "gen:icons": "node scripts/gen-icons.js",
    "gen:demo-catalog": "node scripts/gen-demo-catalog.js",
    "postinstall": "npm --prefix web install --no-audit --no-fund"
  },
  "dependencies": {
    "bcryptjs": "^2.4.3",
    "compression": "^1.7.5",
    "cors": "^2.8.5",
    "exceljs": "^4.4.0",
    "express": "^4.21.2",
    "helmet": "^8.0.0",
    "jsonwebtoken": "^9.0.2",
    "pg": "^8.13.1",
    "qrcode-generator": "^2.0.4"
  }
}
